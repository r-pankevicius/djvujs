// TODO: Quick&dirty solution: you need to recompile to change options.
// Need to implement passing options via DjVuViever ctor

const options = {
    helpPage: true,
    fullPageSwitch: true
};

export default options;